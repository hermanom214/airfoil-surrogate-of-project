forceCoeffs1
{
    type            forceCoeffs;
    libs            ("libforces.so");

    writeControl    timeStep;
    writeInterval   1;

    patches         (airfoil);

    rho             rhoInf;
    rhoInf          1.225;

    liftDir         (0 1 0);
    dragDir         (1 0 0);
    pitchAxis       (0 0 1);

    magUInf         15.000000;

    lRef            1.0;
    Aref            1.0;

    CofR            (0 0 0);
}